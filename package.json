{
  "name": "video-downloader-mv3-css",
  "version": "1.0.0",
  "description": "SCSS compilation for Video Downloader MV3",
  "scripts": {
    "build-css": "sass extension/popup/popup.scss extension/popup/popup.css --style=expanded --no-source-map",
    "watch-css": "sass extension/popup/popup.scss extension/popup/popup.css --watch --style=expanded --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
